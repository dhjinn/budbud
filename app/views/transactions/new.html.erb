<%= simple_form_for @transaction, :url => create_path do |f| %>
  <%= f.collection_select(:club_name, Admin.all, :club_name, :club_name) %>
  <%= f.input :date %>
  <%= f.input :value %>
  <%= f.input :notes %>


  <%= f.button :submit %>
<% end %>

<%= link_to "Cancel", users_url, :class => "button medium"%>